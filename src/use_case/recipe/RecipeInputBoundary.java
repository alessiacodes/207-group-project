package use_case.recipe;

public interface RecipeInputBoundary {
    /// TODO: this is probably not right/enough, re-check CA Coding for inputBoundaries of other use cases
    void execute(RecipeInputData inputData);
}
